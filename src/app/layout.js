import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Best Driving School in Brampton | STEP IN DRIVING SCHOOL | Affordable & Professional",
  description: "Looking for a reliable driving school in Brampton? STEP IN DRIVING SCHOOL offers affordable driving lessons, driving test preparation, and specialized manual driving lessons. Get behind the wheel with confidence. Book your lesson today!",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
